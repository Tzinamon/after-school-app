import React from 'react';
import './ClassBox.css';


function ClassBox({classes, Class}) {
    return (
        <div key={classid} className ="default-box">
            {schedule.time}
            {cname}            
        </div>
    );
}

export default ClassBox;
